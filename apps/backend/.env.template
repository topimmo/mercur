# ========================================
# Server Configuration
# ========================================
# HOST: Network interface to bind to (use 0.0.0.0 for Railway/Docker, localhost for local dev)
HOST=0.0.0.0
# PORT: Server port (Railway will override this with their assigned port)
PORT=9000

# ========================================
# Database
# ========================================
# DATABASE_URL: PostgreSQL connection string (Railway auto-generates this when you add Postgres)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/marketplace
# REDIS_URL: Optional Redis connection for caching/sessions
REDIS_URL=redis://localhost:6379

# Database Connection Pool Settings
# DB_POOL_MIN: Minimum number of database connections in the pool (default: 0)
# DB_POOL_MAX: Maximum number of database connections in the pool (default: 5)
# Note: Lower values help prevent connection pool exhaustion on Railway
DB_POOL_MIN=0
DB_POOL_MAX=5

# ========================================
# CORS Configuration
# ========================================
# Define allowed origins for each application type
# For production, use your actual domain URLs (comma-separated for multiple)
STORE_CORS=http://localhost:3000
ADMIN_CORS=http://localhost:8000
VENDOR_CORS=http://localhost:7000
AUTH_CORS=http://localhost:9000,http://localhost:8000,http://localhost:7000,http://localhost:3000

# ========================================
# Security Secrets (REQUIRED FOR PRODUCTION)
# ========================================
# Generate secure secrets with: openssl rand -base64 32
# DO NOT use these defaults in production!
JWT_SECRET=supersecret
COOKIE_SECRET=supersecret

# ========================================
# Payment Provider (Stripe)
# ========================================
STRIPE_SECRET_API_KEY=sk_test_dummy

# ========================================
# Search Provider (Algolia)
# ========================================
ALGOLIA_API_KEY=dummy
ALGOLIA_APP_ID=dummy

# ========================================
# Email Provider (Resend)
# ========================================
RESEND_API_KEY=dummy
RESEND_FROM_EMAIL=noreply@example.com

# ========================================
# Application URLs
# ========================================
# Used for cross-linking, emails, webhooks, etc.
ADMIN_PANEL_URL=http://localhost:8000
VENDOR_PANEL_URL=http://localhost:7000
STOREFRONT_URL=http://localhost:3000
BACKEND_URL=http://localhost:9000

# ========================================
# Optional: S3 File Storage
# ========================================
# Uncomment and configure to use S3 for file uploads
# S3_ACCESS_KEY_ID=your-access-key
# S3_SECRET_ACCESS_KEY=your-secret-key
# S3_REGION=us-east-1
# S3_BUCKET=your-bucket-name
# S3_FILE_URL=https://your-bucket.s3.amazonaws.com
# S3_ENDPOINT=https://s3.us-east-1.amazonaws.com

# ========================================
# Optional: Development Settings
# ========================================
# NODE_ENV=development
# LOG_LEVEL=debug
